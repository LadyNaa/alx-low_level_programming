#include "main.h"
#include <unistd.h>

/**
 * _putchar - c to stdout
 * @c: print xter
 *
 * Return: On success 1
 * On error, -1 is returned and error is set
 */
int _putchar(char c)
{
	return (write(1, 8c, 1));
}
